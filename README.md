# Nrwl Homebrew Tap

Homebrew formulae for Nrwl's command-line tools.

```sh
brew install nrwl/tap/<formula>
```

Formulae here are maintained automatically by each tool's release pipeline
(via Homebrew's `brew bump-formula-pr`); changes land through pull requests.
